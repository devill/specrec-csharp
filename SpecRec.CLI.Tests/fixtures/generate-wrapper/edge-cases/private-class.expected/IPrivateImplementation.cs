using System;

namespace EdgeCases
{
    // Private interface generated alongside the private wrapper
    private interface IPrivateImplementation
    {
        void DoSomething();
        string GetValue();
    }
}